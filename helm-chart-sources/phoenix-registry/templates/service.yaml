apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-registry.fullname" . }}
  labels:
    {{- include "phoenix-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-registry.selectorLabels" . | nindent 4 }}
