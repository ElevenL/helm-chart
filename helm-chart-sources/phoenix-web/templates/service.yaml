apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-web.fullname" . }}
  labels:
    {{- include "phoenix-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-web.selectorLabels" . | nindent 4 }}
