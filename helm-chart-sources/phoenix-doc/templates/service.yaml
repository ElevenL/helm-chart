apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-doc.fullname" . }}
  labels:
    {{- include "phoenix-doc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-doc.selectorLabels" . | nindent 4 }}
