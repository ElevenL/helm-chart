apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-wx.fullname" . }}
  labels:
    {{- include "phoenix-wx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-wx.selectorLabels" . | nindent 4 }}
