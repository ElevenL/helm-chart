apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-openapi-service.fullname" . }}
  labels:
    {{- include "phoenix-openapi-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-openapi-service.selectorLabels" . | nindent 4 }}
