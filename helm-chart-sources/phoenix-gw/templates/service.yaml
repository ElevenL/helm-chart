apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-gw.fullname" . }}
  labels:
    {{- include "phoenix-gw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-gw.selectorLabels" . | nindent 4 }}
