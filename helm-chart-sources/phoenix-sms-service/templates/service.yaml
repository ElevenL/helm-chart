apiVersion: v1
kind: Service
metadata:
  name: {{ include "phoenix-sms-service.fullname" . }}
  labels:
    {{- include "phoenix-sms-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phoenix-sms-service.selectorLabels" . | nindent 4 }}
